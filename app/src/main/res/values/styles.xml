<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
<!--        <item name="colorOnSurface">@color/gray</item>-->

        <item name="toolbarStyle">@style/Widget.Custom.Toolbar</item>

        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="itemPickerStyle">@style/Widget.Custom.Button.Picker</item>
        <item name="bottomSheetDialogTheme">@style/ThemeOverlay.Myd.BottomSheetDialog</item>

    </style>

</resources>
